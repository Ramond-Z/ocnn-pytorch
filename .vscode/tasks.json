{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run benchmark in Docker",
            "type": "shell",
            "command": "docker compose run --rm benchmark",
            "problemMatcher": [],
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}